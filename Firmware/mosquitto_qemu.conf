# Mosquitto MQTT broker config for LinKey QEMU tests.

per_listener_settings false
allow_anonymous true

listener 1883 0.0.0.0
protocol mqtt

log_dest stdout
log_timestamp true
log_type all

persistence false
